[package]
name = "malleus"
version = "0.2.0"
edition = "2024"
rust-version = "1.93"
license = "MIT OR Apache-2.0"
description = "Structured compiler IR and deterministic reference execution for local numerical kernels"
repository = "https://github.com/akiselev/malleus"
readme = "README.md"
keywords = ["compiler", "kernel", "numerics", "simulation"]
categories = ["compilers", "science"]

[dependencies]
